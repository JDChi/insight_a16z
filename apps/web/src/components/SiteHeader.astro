<header class="site-header">
  <div class="container header-inner">
    <a href="/" class="brand">
      <span class="brand-mark">a</span>
      <span>
        <strong>a16z AI</strong>
        <small>中文洞察站</small>
      </span>
    </a>

    <nav class="nav">
      <a href="/articles">文章</a>
      <a href="/topics">专题</a>
      <a href="/digests">周报</a>
      <a href="/archive">归档</a>
      <a href="/admin">后台</a>
    </nav>
  </div>
</header>

<style>
  .site-header {
    position: sticky;
    top: 0;
    z-index: 10;
    padding: 18px 0;
    backdrop-filter: blur(18px);
    background: rgba(247, 241, 232, 0.75);
    border-bottom: 1px solid rgba(32, 25, 19, 0.08);
  }

  .header-inner {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 24px;
  }

  .brand {
    display: inline-flex;
    align-items: center;
    gap: 14px;
    font-family: var(--font-ui);
  }

  .brand strong,
  .brand small {
    display: block;
  }

  .brand small {
    color: var(--muted);
  }

  .brand-mark {
    width: 42px;
    height: 42px;
    border-radius: 14px;
    display: grid;
    place-items: center;
    background: linear-gradient(135deg, #c96b28, #8a3612);
    color: white;
    font-family: var(--font-display);
    font-size: 1.6rem;
    box-shadow: 0 12px 32px rgba(160, 73, 21, 0.25);
  }

  .nav {
    display: flex;
    flex-wrap: wrap;
    gap: 16px;
    font-family: var(--font-ui);
    font-size: 0.95rem;
  }

  .nav a {
    color: var(--muted);
  }

  @media (max-width: 720px) {
    .header-inner {
      flex-direction: column;
      align-items: flex-start;
    }
  }
</style>
